#!/usr/bin/env python

from setuptools import setup

setup(name="cpos",
      version="0.1",
      packages=["cpos",
                "cpos.p2p",
                "cpos.test"],
      scripts=[],
      package_data={},
      )
